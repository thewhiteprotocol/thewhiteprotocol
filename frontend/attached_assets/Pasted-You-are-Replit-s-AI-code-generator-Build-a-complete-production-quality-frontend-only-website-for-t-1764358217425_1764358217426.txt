You are Replit’s AI code generator. Build a complete, production-quality frontend-only website for the pSol Protocol.

CONTEXT & GOAL
- pSol Protocol is a privacy pool protocol on Solana.
- The core on-chain program (Phase 3) is implemented and deployed on Solana devnet.
- The protocol is currently devnet-only; mainnet will come after audits.
- This website is the public entry point: overview, documentation, roadmap, links, and basic devnet status.
- There is NO backend for now. Everything must be frontend-only and safe to host as static files.
- Later we will plug in a TypeScript SDK, but not in this version.

STACK & PROJECT SETUP
- Use: React + Vite + TypeScript.
- Use: Tailwind CSS for styling (configure PostCSS + Tailwind properly).
- Directory structure (standard Vite React + TS app):
  - `index.html`
  - `src/main.tsx`
  - `src/App.tsx`
  - `src/components/*`
  - `src/sections/*`
  - `src/assets/*`
  - `src/styles/*` if needed.
- No backend, no server-side framework. Pure SPA.

DESIGN & UX
- Professional, clean, protocol-style design. No memes, no emojis.
- Dark theme with subtle accent color (e.g. cyan/teal for “pSol”).
- Responsive: works well on desktop and mobile.
- Use a single-column layout (centrally aligned sections), no multi-column text blocks that are hard to copy.
- Smooth scroll navigation between sections.
- Simple sticky top navigation bar with these anchors:
  - “Overview”
  - “Architecture”
  - “Docs”
  - “Devnet”
  - “Roadmap”
  - “Links”

CONTENT SECTIONS (ALL IN ONE PAGE)

1) HERO / OVERVIEW SECTION
- Title: “pSol Protocol – Private Liquidity on Solana”
- Short subtitle: 1–2 lines explaining:
  - pSol is a privacy pool protocol on Solana,
  - currently live on devnet,
  - Phase 3 core protocol is implemented (deposit, withdraw, private transfers with Groth16 proofs).
- Prominent badge: “Status: Phase 3 complete – deployed on Solana devnet”
- Buttons:
  - “Read the docs” → scrolls to Docs section.
  - “View on GitHub” → external link to: https://github.com/psolprotocol/psol-v1
- Small line mentioning: “Twitter / X: @psolprotocol” linking to https://x.com/psolprotocol

2) ARCHITECTURE SECTION
A section with concise technical description. Use normal HTML text, not images.

Explain:
- pSol is an Anchor-based Solana program.
- Core features:
  - Deposit SPL tokens into a privacy pool,
  - 2-in / 2-out private transfers using Groth16 proofs,
  - Withdraw back to transparent SPL accounts.
- Cryptography model:
  - Commitments and nullifier hashes are computed off-chain with Poseidon,
  - Merkle tree parent hashes use Keccak256 on-chain,
  - Groth16 proof verification on BN254 (via Solana’s alt_bn128 precompiles),
  - Verification keys stored on-chain with curve and identity checks.
- Devnet only for now; mainnet is planned after Phase 4 security hardening and audits.

3) DOCS SECTION (DEVELOPER-ORIENTED)
Create a Documentation section with subsections as plain text blocks. No markdown files; just structured headings and paragraphs.

Subsections:
- “Getting Started”
  - Prerequisites:
    - Solana CLI,
    - Anchor CLI,
    - Rust toolchain,
    - Node + Yarn or NPM.
  - High-level steps to build locally:
    - `git clone https://github.com/psolprotocol/psol-v1.git`
    - `cd psol-v1`
    - `anchor build`
- “Program Overview”
  - Mention the key accounts:
    - PoolConfig,
    - MerkleTree,
    - VerificationKeyAccount,
    - SpentNullifier accounts,
    - Vault account.
  - Mention the key instructions:
    - `deposit`,
    - `withdraw`,
    - `private_transfer`,
    - `set_verification_key`,
    - admin instructions like `pause` / `unpause` (briefly).
- “Devnet Deployment”
  - Explain that the protocol is currently deployed on Solana devnet.
  - Mention that the frontend interacts directly with devnet RPC using the program ID and IDL.
  - IMPORTANT: Do NOT hardcode a fake program ID; instead, create a clear placeholder in the UI text where we can later paste the real devnet program ID, like: “Program ID: <REPLACE_WITH_DEVNET_PROGRAM_ID>”.
- “How to Interact (CLI / Scripts)”
  - Describe, in text, that developers can:
    - Use Anchor scripts or Solana web3.js to call deposit/withdraw/private_transfer,
    - Use off-chain Poseidon + Groth16 to prepare commitments, nullifiers, and proofs,
    - Submit transactions with the generated proof bytes.
  - Keep the instructions high-level and accurate, not pseudo-magic.

4) DEVNET STATUS SECTION
This section is a simple frontend-only view; no backend.

- Show:
  - “Network: Solana devnet”
  - “Program: pSol Privacy Protocol”
  - A placeholder for “Program ID: <REPLACE_WITH_DEVNET_PROGRAM_ID>”
- Add a “Connect Wallet” button using Solana Wallet Adapter (Phantom, Solflare, etc.):
  - Integrate `@solana/wallet-adapter-react` and `@solana/wallet-adapter-wallets`.
  - Devnet endpoint: `https://api.devnet.solana.com`.
  - For now, this section only:
    - Allows the user to connect a wallet on devnet,
    - Shows their public key and current connection status.
  - Do NOT implement actual deposit/withdraw UI yet. Just connection and network display. We will add interactive flows later when the SDK is ready.

5) ROADMAP SECTION
Text-only section with clearly separated phases. Short, precise descriptions.

List 5 phases:

- Phase 1 – Prototype:
  - Initial PoC for deposit and withdraw on Solana.
- Phase 2 – Security Skeleton:
  - Clean Anchor structure, state models (pool config, merkle tree, nullifiers, vault).
- Phase 2.5 – Cryptographic Wiring:
  - Real Groth16 verifier integration,
  - Poseidon-based commitments and nullifiers,
  - Merkle tree + root history on-chain.
- Phase 3 – Core Privacy Protocol (Current):
  - Fully implemented private transfers (2-in / 2-out),
  - Groth16 proof verification enforced on-chain (no dev bypass),
  - Verification key validation, nullifier and Merkle invariants.
  - This is the current devnet deployment.
- Phase 4 – Security Hardening:
  - Additional tests, invariants, logging,
  - Audit preparation, improved docs.
- Phase 5 – SDK and Ecosystem:
  - TypeScript SDK for easy integration,
  - Example dApps and end-to-end flows,
  - Mainnet deployment after audit.

6) LINKS SECTION
Plain link list with buttons:

- “GitHub Repository” → https://github.com/psolprotocol/psol-v1
- “Twitter / X” → https://x.com/psolprotocol
- Optional: placeholder “Docs (coming soon)” for future dedicated docs site.

IMPLEMENTATION DETAILS

- Use functional React components with TypeScript types.
- Use Tailwind utility classes to style:
  - A main layout wrapper,
  - A sticky top navbar,
  - Sections with consistent vertical spacing,
  - Headings, paragraphs, and buttons.
- Organize components by sections:
  - `src/sections/Hero.tsx`
  - `src/sections/Architecture.tsx`
  - `src/sections/Docs.tsx`
  - `src/sections/DevnetStatus.tsx`
  - `src/sections/Roadmap.tsx`
  - `src/sections/Links.tsx`
  - `src/components/Navbar.tsx`
- In `App.tsx`, compose the sections in order with IDs for scroll navigation.
- Implement smooth scrolling to section IDs when user clicks on navbar links.
- Do not use any placeholder ABSURD fake text. The content should be realistic, concise, and match the technical description above.

IMPORTANT CONSTRAINTS

- No backend, no server code. Frontend-only SPA.
- Do NOT hardcode fake program IDs. Use a placeholder string with clear “REPLACE_WITH_DEVNET_PROGRAM_ID”.
- All text should be professional and clear (no emojis, no jokes, no filler).
- Keep all content in a single-column friendly layout so it is easy to read and copy.

Finally, generate all necessary files (Vite config, Tailwind config, tsconfig, package.json, etc.) so the project runs with:

- `npm install`
- `npm run dev`
- `npm run build`

inside Replit.
